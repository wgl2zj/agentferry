# AGENTS.md — 核心执行规则（唯一权威入口）

> 本文件是项目规则的**唯一人工维护源**。细则下沉到 `.rules/`；本文件只保留所有任务通用的铁律、任务路由和执行入口。

## 优先级标记

- **【强制】**：不可违反；违反时不得汇报"完成"。
- **【应当】**：强烈建议；偏离时必须说明理由。
- **【可选】**：按任务需要执行。
- 【强制】只有以上显式标记决定规则等级；正文中的"必须、禁止、不得"等自然语言只表达动作，不单独形成新的优先级。

---

## 一、通用铁律

1. 【强制】全程使用团队约定语言对话与写注释（本项目为中文），回答简洁、直接。
2. 【强制】开发在本仓库功能分支进行；打 tag、发布 GitHub Release（视为上线）必须取得用户明确授权。
3. 【强制】不可逆操作（覆盖目标资产、删除文件、发布版本）必须先产出变更计划（dry-run 清单）并取得用户确认。
4. 【强制】发布版本必须取得用户授权，并先汇报计划与影响评估。
5. 【强制】涉及用户家目录下的 agent 资产源目录（如 `~/.zcode/`、`~/.claude/` 等被迁移对象）、含登录凭据的文件（如 `credentials.json`）、`.env` 及备份目录等敏感路径时，默认只读；必要读取先说明原因，任何写入或打包必须取得用户明确授权。
6. 【强制】回复和文档中的代码名、变量名、字段名、表名、函数名等代码式标识，在首次出现和关键位置补充中文含义；连续重复且语义清楚时可省略。
7. 【强制】任务是否完成，以命中的任务清单、验证结果和偏离自检为准，不以"代码已写完"单独判定。

## 一点五、功能地图维护

- 【强制】`.rules/FEATURE_MAP.md`（功能地图）记录各功能模块的"行为预期"（该做什么，可验证）和"已知待修问题"（哪里与该做的不一致），用于发现"以为≠实际"的偏差、防止改 A 误伤 B。
- 【强制】只记变化慢的内容：**行为预期**与**待修问题**入地图；实现细节（具体函数、字段名等会随代码天天改的东西）不入。
- 【强制】修改某模块代码时，必须先读该模块在功能地图里的条目，对照当前代码核对行为预期：
  - 当本轮改动**改变了模块的对外行为**，在"修改后"提交里同步更新对应预期。
  - 当发现**代码与地图预期不一致**（地图过期，或代码有隐藏偏差），当场修正地图使其与代码一致；属于真 BUG 的另记入"待修问题"。
- 【强制】使用功能地图作为判断依据前，必须回到代码核实当前行为，不得直接采信地图文字。
- 【应当】新功能上线后，在该模块新增一条功能地图条目（职责 + 行为预期）。
- 【强制】新功能/新模块（新增页面、组件、核心引擎、命令行入口等；小修小补除外）开发前，必须先向用户输出"行为预期清单"（措施：先写预期再实现）：每条预期**可验证**、带状态标注（待实现/已实现/已修复），用户确认预期后再动手实现；实现后按预期逐条验收，**配测试锁住预期**并更新状态。预期未经测试锁住只有提醒作用，锁住才是机器保障；验收不通过不得汇报完成。

## 二、任务前置自检

**触发判定**：本轮计划主动修改、新增或删除仓库文件时，必须先声明前置自检；只读、纯问答和闲聊任务豁免。已有 `git status`（工作区状态）变化不改变本判定。

动手前输出：

```text
【前置自检】
- 任务类型：<代码修改/测试/方案实施/纯文档修改/…>
- 必读规则：<列出本轮完整加载的规则文件>
- 必做动作：<列出本任务命中的完成清单和验证动作>
```

执行纪律：

- 【强制】声明在前，实质性修改在后。
- 【强制】必做动作只引用本任务实际命中的完成清单，不得把发版清单套用于普通代码或纯文档修改。
- 【强制】涉及视觉类子任务（UI 实现/验收、看图、OCR、前端验证）时，按用户级 AGENTS.md 的视觉任务路由规则在规划期完成分派，本项目规则不单独判定验证方式。

## 三、任务路由（按优先顺序、可累加）

任务从上到下判定；同时命中多项时，完整加载所有命中的细则。

| 优先级 | 任务类型 | 判定信号 | 必读细则 |
|---|---|---|---|
| 1 | 方案/设计文档实施 | 用户要求实施方案，或任务含"一步切换、替换事实来源、停用旧入口"等约束 | `.rules/MIGRATION_VERIFICATION_DISCIPLINE.md`（方案防偏离规则）全文；再加载对应编码规则 |
| 2 | 排障/调试 | 功能异常、测试失败、性能回退、现象与预期矛盾 | `.rules/DEBUG_LESSONS.md`（调试规则）全文；需要修复代码时再加载编码规则 |
| 3 | 普通代码修改/测试 | 修改源码或测试代码 | `.rules/CODING_STANDARDS.md`（编码与测试规则）全文 |
| 4 | 前端/UI 设计 | 修改组件、样式或用户可见的脚本，新增页面、组件、交互反馈或可访问性调整 | `.rules/UI_DESIGN.md`（UI 设计规则）全文；同时加载编码规则 |
| 5 | 纯文档/治理配置修改 | 只改 Markdown、规则、索引或不影响程序运行的治理配置 | `.rules/DOCUMENTATION.md`（文档治理规则）全文 |
| 6 | issue/工具链任务 | 创建 issue、拆 ticket、发 spec | 读 `docs/agents/issue-tracker.md`；配合 to-tickets / to-spec skills；同时遵循本文件通用铁律 |

兜底：

- 【强制】判定不清是否为方案实施时，按方案实施处理。
- 【应当】判定不清是否为排障时，按排障处理。
- 【强制】不得用"日常小调整"绕过编码、调试或方案细则。

## 四、完成清单入口

- 普通代码修改/测试：`.rules/CODING_STANDARDS.md`（编码与测试规则）"任务完成清单"。
- 前端/UI 设计：`.rules/UI_DESIGN.md`（UI 设计规则）"UI 任务完成清单"，同时执行编码完成清单。
- 排障/修复：`.rules/DEBUG_LESSONS.md`（调试规则）"调试纪律红线"和清理要求，同时执行编码完成清单。
- 方案实施：`.rules/MIGRATION_VERIFICATION_DISCIPLINE.md`（方案防偏离规则）四道纪律。
- 纯文档/治理配置：`.rules/DOCUMENTATION.md`（文档治理规则）"文档任务完成清单"。
- issue/工具链任务：无独立完成清单；遵循命中任务的通用铁律与对应完成清单。

## 五、日常开发与 Git 流程

- 【强制】日常开发、调试、测试全部在本仓库功能分支进行；构建产物（`target/`、`dist/`、安装包）不入库。
- 【强制】每轮将要修改文件或新增测试前，先提交当前工作区，提交信息前缀固定为 `修改前:`；无可提交改动时执行 `git commit --allow-empty -m "修改前:<说明>"`（空检查点提交）。
- 【强制】完成修改并跑完必要验证后，再提交一次，前缀固定为 `修改后:`；该提交只包含本轮实际改动相关文件。
- 【强制】配置远程仓库后，每次本地提交同步推送到远程；尚未配置远程时在汇报中注明"未推送（无远程）"。
- 【强制】完成后向用户汇报：改了什么、是否提交和推送、跑了哪些测试或校验、是否需要用户手动验证。

## 六、测试运行规则

- 【强制】任何情况下都不得自主运行全量回归。是否全量回归由用户决定，AI 只提建议不执行。全量回归命令：`npm test && cargo test --manifest-path src-tauri/Cargo.toml`。
- 【应当】日常小范围修改优先运行直接相关测试，并在汇报中说明跑了哪些、为什么跑这些。
- 【应当】当本次改动触及打包/覆盖/同步核心引擎、文件校验逻辑、Tauri 命令边界、跨模块共享接口、公共基础服务，或临近发版，认为有必要全量回归时，**不要自行执行**，而是在任务结束的汇报中提出"建议进行全量回归"并说明理由，由用户决定是否执行。
- 详细测试规范见 `.rules/CODING_STANDARDS.md`「测试规范」节。

## 七、方案实施防偏离

【强制】方案实施必须执行 `.rules/MIGRATION_VERIFICATION_DISCIPLINE.md`（方案防偏离规则）的完成定义、禁用清单、反向测试和偏离自检；日常 Bug 修复、小范围功能调整和纯查询不适用。

## 八、规则单一权威源与镜像维护

- 【强制】`AGENTS.md` 是唯一人工维护源；不得在本文件之外（任何其他 AI 工具的配置文件）手工维护第二份规则。
- 【强制】不得只修改其他配置文件形成独立规则。项目当前不使用镜像；如未来引入镜像（如 `CLAUDE.md`），镜像必须由脚本从本文件同步生成，不得手工维护。
- 【强制】`## Agent skills` section（含其指向的 `docs/agents/*.md`）同属本文件正文，由 `setup-project-rules-matt` 初始化、人工维护；如使用镜像，镜像同步脚本须将其一并写入镜像文件，不得在镜像里单独维护第二份工具链配置。

## Agent skills

> 本节是 engineering skills 的工具链配置附录，与上方纪律规则同属本文件正文（AGENTS.md 是唯一权威源）。
> 维护归属：由 `setup-project-rules-matt` 初始化，之后人工维护。
> 本节只记录"工具链怎么用"；"任务怎么算完成"仍以上方各节纪律和 `.rules/` 完成清单为准。

### Issue tracker

Issues 与 specs 以 Markdown 文件存放在仓库 `.scratch/` 目录（本地文件 tracker，不依赖远程服务；未来开源时可迁移到 GitHub Issues）。详见 `docs/agents/issue-tracker.md`。

### Domain docs

Single-context 仓库：领域术语见根目录 `CONTEXT.md`、架构决策见 `docs/adr/`（两者由 `/domain-modeling` 懒创建，暂缺时静默继续）。详见 `docs/agents/domain.md`。
